#include <iostream>
using namespace std;

int main() {
    char str[100];
    cout << "Enter a sentence: ";
    cin.get(str, 100);
    for (int i = 0; str[i] != '\0'; i++) 
    {
        if (str[i] != ' ')
            cout << str[i];
    }
    return 0;
}
